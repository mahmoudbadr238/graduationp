# Sentinel Linux Dependencies
PySide6>=6.5.0
psutil>=5.9.0
requests>=2.28.0
groq>=0.4.0
python-dotenv>=1.0.0
yara-python>=4.3.0

# GPU monitoring (optional - install if GPU monitoring is needed)
# pynvml>=11.5.0
# pyadl>=0.1

# Build tool (only needed if you want to create a standalone binary)
# pyinstaller>=6.0
