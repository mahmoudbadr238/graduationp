# ═══════════════════════════════════════════════════════════════════════════════
# Sentinel - Endpoint Security Suite
# Python Dependencies
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────────
# CORE FRAMEWORK
# ─────────────────────────────────────────────────────────────────────────────────
PySide6>=6.6.0                  # Qt 6 framework for GUI (QML + Python)

# ─────────────────────────────────────────────────────────────────────────────────
# SYSTEM MONITORING
# ─────────────────────────────────────────────────────────────────────────────────
psutil>=5.9.0                   # Cross-platform system monitoring (CPU, RAM, disk, network)
WMI>=1.5.1                      # Windows Management Instrumentation (Windows system info)

# ─────────────────────────────────────────────────────────────────────────────────
# WINDOWS INTEGRATION (Windows only)
# ─────────────────────────────────────────────────────────────────────────────────
pywin32>=306; sys_platform=='win32'     # Windows API access (Event Logs, Registry)

# ─────────────────────────────────────────────────────────────────────────────────
# NETWORKING & HTTP
# ─────────────────────────────────────────────────────────────────────────────────
requests>=2.31.0                # HTTP client for API calls
urllib3>=2.0.0                  # HTTP library (requests dependency)
aiohttp>=3.9.0                  # Async HTTP client for Groq/AI providers

# ─────────────────────────────────────────────────────────────────────────────────
# GPU MONITORING (Optional - gracefully degraded if unavailable)
# ─────────────────────────────────────────────────────────────────────────────────
pynvml>=11.0.0                  # NVIDIA GPU monitoring (NVIDIA GPUs)
GPUtil>=1.4.0                   # GPU utilities wrapper
pyadl>=0.1                      # AMD GPU monitoring (AMD GPUs on Windows)
# Note: amdsmi is installed separately if needed for Linux AMD GPUs

# ─────────────────────────────────────────────────────────────────────────────────
# AI / ONLINE AI (Groq is default free tier, Claude/OpenAI as fallback)
# ─────────────────────────────────────────────────────────────────────────────────
# Groq uses standard HTTP API - no special SDK needed
anthropic>=0.39.0               # Claude AI API client (fallback)
openai>=1.54.0                  # OpenAI API client (fallback)
# Configure via environment variables:
#   ANTHROPIC_API_KEY=sk-ant-...  (for Claude)
#   OPENAI_API_KEY=sk-...         (for OpenAI, fallback if Claude not available)

# ─────────────────────────────────────────────────────────────────────────────────
# AI / LOCAL LLM (Optional - kept for offline fallback, NOT recommended)
# ─────────────────────────────────────────────────────────────────────────────────
# transformers>=4.35.0          # HuggingFace transformers for local LLM
# onnxruntime-gpu>=1.16.0       # ONNX Runtime with GPU support (CUDA/TensorRT)
# optimum[onnxruntime-gpu]>=1.14.0  # HuggingFace Optimum with ONNX GPU support
numpy>=1.24.0                   # Required for tensor operations
# Note: Local LLM is DEPRECATED - use online AI for production quality

# ─────────────────────────────────────────────────────────────────────────────────
# CONFIGURATION & UTILITIES
# ─────────────────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # Environment variable management (.env files)
colorama>=0.4.6                 # Cross-platform colored terminal output
imageio>=2.36.0                 # GIF export for Sandbox Lab proof media


# ─────────────────────────────────────────────────────────────────────────────────
# SECURITY SCANNING (Optional - enhanced analysis if available)
# ─────────────────────────────────────────────────────────────────────────────────
pefile>=2023.2.7                # PE file parsing for Windows executables (optional)
yara-python>=4.3.0              # YARA rule matching for malware detection (optional)
pywebview>=4.0                  # WebView2 bindings for URL sandbox detonation (optional)
# Note: yara-python requires Visual C++ Build Tools on Windows
# Note: These are optional - the app works without them but with reduced analysis

# ─────────────────────────────────────────────────────────────────────────────────
# DATABASE
# ─────────────────────────────────────────────────────────────────────────────────
# sqlite3 is included in Python standard library - no external dependency needed

# ═══════════════════════════════════════════════════════════════════════════════
# DEVELOPMENT DEPENDENCIES (Optional - not needed for running the app)
# ═══════════════════════════════════════════════════════════════════════════════

# Testing
pytest>=8.0.0                   # Testing framework
pytest-qt>=4.2.0                # Qt/PySide6 testing support

# Code Quality
ruff>=0.1.0                     # Fast Python linter and formatter
mypy>=1.0.0                     # Static type checker
bandit>=1.7.0                   # Security linter

# Type Stubs
types-psutil>=5.9.0             # Type stubs for psutil
types-pywin32>=306              # Type stubs for pywin32
types-requests>=2.31.0          # Type stubs for requests

# Build & Packaging
pyinstaller>=6.0.0              # Executable bundler
